			<div style="margin:10px 0">
				{{#if name}} 
				<div class="breadCrumbSeparator">
				<a href="javascript:app.showAllDirectMessages()">Direct messages</a>  

				<li class="fa fa-long-arrow-right"></li></div>  with {{name}}
				&nbsp;&nbsp;
					{{#if publicKey}}
					<!--<br/>{{name}} sent you an encryption key on {{publicKey.displayDate}}. -->
					{{/if}}
				<br/>Not sure if {{name}} has your public key?  <input type=button onClick="app.initiateDirectMessages({{id_str}},true)" value="send it"/>
				{{/if}}
				{{#if sentKeyWarning}}
				<br/>Your current key is newer than the key you last sent to {{name}}. 
				{{/if}}
			</div>
